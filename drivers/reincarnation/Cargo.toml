[package]
name = "reincarnation"
version = "0.1.0"
edition = "2024"

[dependencies]
jon_common = { path = "../common" }
spinning_top = "0.3.0"
buddy_system_allocator = "0.11.0"

[profile.dev]
panic = "abort"
debug = true

[profile.release]
panic = "abort"
